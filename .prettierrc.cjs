/** @type {import('prettier').Config} */
const config = {
    semi: true,
    tabWidth: 2,
    trailingComma: "all",
    singleQuote: false,
    arrowParens: "always",
    printWidth: 100,
  };

  module.exports = config;
